# 验收记录

## v0.2 简历助手循环与求职偏好

- 49 项测试（含本地 HTML 事件处理测试）：通过。
- 两轮演示循环：确认修改生成新底稿 → 重新匹配 → 相同岗位对比，通过。
- 城市/薪酬/行业/经验学历等条件、本地缺失值处理、网页映射回读模拟，通过。
- 新岗位或新求职条件不计入相同基准的简历改进，已测试。
- HTML 脚本语法与配置/建议导出事件已验证；当前云浏览器访问本地报告返回 ERR_BLOCKED_BY_CLIENT，未完成真实浏览器视觉验收。
- BOSS 实际控件筛选、真实账号岗位采集、模型 API 和用户简历尚未验收。

以下为 v0.1 基线记录。

## 已执行

- 22 项 Python unittest：全部通过。
- 三个演示岗位从 JSON 导入 → 筛选 → HTML/JSON/招呼语导出 → SQLite 状态查询 → CSV 导出：通过。
- 演示结果：匹配 1、待核实 1、排除 1，与预期一致。
- Python 模块编译与浏览器扩展 JavaScript 语法检查：通过。

## 未执行

- BOSS、猎聘真实账号登录、岗位采集与申请发送。
- 真实模型 API 调用（需要用户自行配置接口）。
- 浏览器扩展商店发布和真实页面人工验收。
- 真实简历 Word/PDF 排版验收。

## 上线前必须补齐的业务验证

1. 用户简历事实核对、目标城市/薪资/岗位范围。
2. 真实岗位公司与岗位标题定位，跳转到聊天页后目标身份复核。
3. 站点附件选择是否支持逐岗位版本；如果只能使用公共附件，需要改为独立发送流程。
4. 消息发送回执、附件发送回执分别验证。
5. 登录过期、验证码、频率限制出现时能够停止。

当前不能将本版本描述为“BOSS/猎聘全自动投递已可用”。
