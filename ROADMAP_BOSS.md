# BOSS 全流程操作助手：范围与优化计划

2026-09-12 主线更新：v0.3 已实现“简历 → BOSS/猎聘分平台搜索 → 精确条件复核 → 去重市场信号 → 修改建议 → 确认 → 新版再匹配”。具体见 `docs/RESUME_ASSISTANT.md` 和 `docs/PLATFORM_BEHAVIOR.md`。下列账号内自动操作仍作为后续建设计划；真实页面适配仍待验收。

目标更新：让本地助手通过可见浏览器页面完成用户日常求职操作，包含搜岗位、看公司、调整材料、沟通、发附件和跟进。优先完成 BOSS 网页端；移动端专属功能不作支持承诺。以下为建设计划，不代表已完成。

## 当前代码检查

1. `adapters/boss.json` 的定位器为占位，steps 为空，尚不能实际投递。
2. `browser.py` 是固定步骤执行器，不能判断未知页面并重新规划；不支持聊天弹窗、多标签页切换与站点分支状态。
3. 公司/岗位身份只在进入岗位页时验证；跳转到聊天或切换会话后未再次核对招聘者身份。发送前必须复核当前会话。
4. 当前回执判断使用文本包含，缺少消息 ID、出现时间和附件版本等绑定。旧回执过滤只能挡住部分误判。
5. uncertainty 能阻止重复投递，但缺少人工核对后的恢复入口及逐步续跑。
6. 每日上限在批次启动时计数，不是跨进程原子限额；需要任务租约和统一调度。
7. 学历、统招性质、年限、行业、薪资周期、福利、活跃度等已可结构化核验；职责层级和复杂语义仍需人工或受约束模型核对。
8. 自动采集虽有执行器，但列表翻页未验证列表确实更新，也未实现持久化采集检查点。
9. 已有“提出改写—对照原文—确认入库—重新匹配”流程；真实回复率与录用结果尚不能归因于改写。
10. 61 项测试是核心逻辑和模拟定位器测试，不能代替真实 BOSS/猎聘页面端到端验收。

## 操作范围

| 模块 | 目标行为 | 首次实施必须核实 |
|---|---|---|
| 登录与会话 | 打开专用浏览器、保留登录、检测过期、暂停后恢复 | 手动登录方式、验证码与风控页面 |
| 求职设置 | 城市、岗位、薪资、行业、排除公司、可到岗时间 | 公开资料修改范围与用户确认过的事实 |
| 搜索 | 输入关键词、选择筛选项、翻页、切换搜索条件 | 当前网页真实控件、列表更新与空结果 |
| 浏览 | 读取岗位、公司、招聘者和要求，收藏、不感兴趣 | 网页功能是否存在及其作用范围 |
| 匹配 | 学历/年限/地域/薪资硬筛，行业与项目证据匹配 | 未知条件转待核实，不自动认定符合 |
| 简历 | 管理基础版和逐岗位版本，提出关键词改写建议 | 公共在线简历与单次附件的区别；不高频覆盖公共简历 |
| 建立沟通 | 点击沟通入口，判断已沟通或需等待状态 | 点击入口本身是否已经自动发送默认消息 |
| 聊天 | 读取当前会话、写开场白、依据事实回答常见问题 | 同名招聘者、切换会话、多职位关联、消息是否真正发出 |
| 发附件 | 选择正确版本、请求发送、等待同意、确认发送 | 上传到资料库不等于发送给对方；支持哪些文件类型 |
| 跟进 | 识别未读回复、维护阶段、生成跟进草稿 | 去重、跟进频次、是否已有人工回复 |
| 面试 | 识别邀约、提取时间地点、记录准备事项 | 自动确认仅限用户预先给定的可用时间与规则 |
| 控制 | 预览、执行一个批次、暂停、接管、重试只读动作 | 页面变化和结果不确定时必须停止发送 |

在既定求职范围内允许自动执行重复动作，不逐条请求确认。薪资承诺、变更求职事实、接受 offer、付费、删除账号/数据等不能从模糊目标自行推断授权。登录验证由用户完成，不绕过限制。

## 推荐架构

- 本机可见浏览器：实际完成点击、填写、滚动、选择文件；专用用户目录保存会话。
- BOSS 页面适配层：识别搜索/详情/聊天/简历/登录/验证等状态，定位页面元素并判断可执行动作。
- 任务调度层：保存任务、租约、次数限制、暂停原因和已完成步骤；保证重启后不重复发送。
- 业务决策层：模型分析岗位与生成草稿，事实库约束输出；模型不直接决定账号安全设置或求职承诺。
- 操作验证层：每次发送前绑定招聘者、岗位与简历版本；发送后验证新的可见回执。
- 本地控制界面：显示当前岗位、浏览器状态、匹配依据、简历差异、下一步动作和接管按钮。

优先用语义定位/DOM 完成稳定动作。复杂画面可考虑截图辅助识别，但识别不确定时暂停，不能盲点坐标。网页中的岗位文本、聊天内容是外部数据，不能当系统指令。

## 建设顺序与验收

### P0：一个岗位的真实闭环

在用户登录的实际 BOSS 页面中验证：搜索 → 岗位详情 → 身份复核 → 个性化开场 → 会话目标复核 → 选定附件 → 发送回执。
逐个记录页面样本与操作路径，不保存无关聊天。先只读验证，再在授权目标岗位执行。
验收：每一个“成功”都能在对应会话找到新增消息/附件；不把沟通入口点击或附件上传当作发送成功。

### P1：稳定批次与接管

新增任务状态机、每步检查点、跨进程额度、消息去重、附件绑定、会话复核、异常恢复及本地控制界面。
验收：会话切错、页面结构变动、登录失效、网络在发送后断开时，不继续盲投；人工核对后仅执行未完成步骤。

### P2：材料管理与匹配质量

补充学历/年限/职责要求解析、证据分项评分、未知条件、简历改写差异审阅与版本管理。
验收：仅有关键词但无真实经历的岗位不能误判为充分匹配；研究规划不会变成已上线成果。

### P3：消息跟进和求职管理

在已有沟通上下文内识别 HR 回复，基于已确认事实自动回答常见问题；未知问题进入待答列表。
增加面试阶段、跟进草稿、跨会话去重和按批次汇总。后台定期执行是单独部署功能，不把聊天窗口本身当长期运行服务。

## 明确不走的实现方式

不沿用 BOSS 私有聊天协议、抓取 Cookie 后在服务器伪造接口请求，或绕过验证码和频率限制。
不同时开启多个独立自动投递器。当前 boss-helper 与 JobPilot 无共享历史，双开可能重复沟通。
不承诺“BOSS 所有功能”已经覆盖；以网页端可见功能清单及逐项实测结果为准。
